DB_HOST=db
DB_USERNAME=enman
DB_PASSWORD=password
DB_NAME=enman_development

RAILS_ENV=development
RAILS_LOG_TO_STDOUT=true
RAILS_SERVE_STATIC_FILES=true

# ActiveRecord::Encryption keys (generate with: bin/rails db:encryption:init)
AR_ENCRYPTION_PRIMARY_KEY=
AR_ENCRYPTION_DETERMINISTIC_KEY=
AR_ENCRYPTION_KEY_DERIVATION_SALT=

# JWT secret key (defaults to Rails secret_key_base if not set)
SECRET_KEY_BASE=

